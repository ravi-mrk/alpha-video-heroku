import { RequestFilter } from "./ActionInteractor";
export declare class VirtualGoogleAssistant {
    static Builder(): VirtualGoogleAssistantBuilder;
    intend(intentName: string, slots?: {
        [id: string]: string;
    }): Promise<any>;
    launch(): Promise<any>;
    utter(utterance: string): Promise<any>;
    addFilter(requestFilter: RequestFilter): VirtualGoogleAssistant;
    resetContext(): VirtualGoogleAssistant;
    resetFilters(): VirtualGoogleAssistant;
    stopExpressServer(): Promise<void>;
    startExpressServer(): Promise<void>;
}
export declare class VirtualGoogleAssistantBuilder {
    private _locale;
    actionUrl(url: string): VirtualGoogleAssistantBuilder;
    handler(handlerName: string | ((...args: any[]) => void)): VirtualGoogleAssistantBuilder;
    expressModule(fileName: string, port: number): VirtualGoogleAssistantBuilder;
    directory(directory: string): VirtualGoogleAssistantBuilder;
    locale(locale: string): VirtualGoogleAssistantBuilder;
    private getInteractor;
    create(): VirtualGoogleAssistant;
}
