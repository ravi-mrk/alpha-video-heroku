import { IIntentSchema, Intent } from "virtual-core";
export declare class GoogleIntent extends Intent {
    action?: string;
}
export declare class IntentSchema implements IIntentSchema {
    private intentArray;
    static fromDirectory(directory: string): IntentSchema;
    constructor(intentArray: Intent[]);
    intents(): Intent[];
    intent(intentString: string): Intent;
    hasIntent(intentString: string): boolean;
    private static getIntentArrayFromJsonFile;
}
