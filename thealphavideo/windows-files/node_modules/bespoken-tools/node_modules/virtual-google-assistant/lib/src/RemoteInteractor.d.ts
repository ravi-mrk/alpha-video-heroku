import { InteractionModel } from "./InteractionModel";
import { ActionInteractor } from "./ActionInteractor";
export declare class RemoteInteractor extends ActionInteractor {
    private urlString;
    protected interactionModel: InteractionModel;
    constructor(urlString: string, interactionModel: InteractionModel, locale: string);
    protected invoke(requestJSON: any): Promise<any>;
}
