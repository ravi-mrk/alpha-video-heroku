import { IModel, SampleUtterances, SlotTypes } from "virtual-core";
import { IntentSchema } from "./IntentSchema";
export declare class InteractionModel implements IModel {
    intentSchema: IntentSchema;
    sampleUtterances: SampleUtterances;
    slotTypes?: SlotTypes;
    dialogFlowApiVersion: string;
    static fromDirectory(directory: string): InteractionModel;
    constructor(intentSchema: IntentSchema, sampleUtterances: SampleUtterances, slotTypes?: SlotTypes, dialogFlowApiVersion?: string);
    hasIntent(intent: string): boolean;
}
