export declare class ExpressServerWrapper {
    private expressFile;
    private expressPort;
    private handlerModule;
    private serverStarted;
    constructor(expressFile: any, expressPort: number);
    startServer(): Promise<void>;
    stopServer(): Promise<void>;
    isServerStarted(): boolean;
}
