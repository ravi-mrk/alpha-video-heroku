import { ActionInteractor } from "./ActionInteractor";
import { InteractionModel } from "./InteractionModel";
export declare class LocalFunctionInteractor extends ActionInteractor {
    private handler;
    protected model: InteractionModel;
    constructor(handler: string | ((...args: any[]) => void), model: InteractionModel, locale: string);
    protected invoke(requestJSON: any): Promise<any>;
}
