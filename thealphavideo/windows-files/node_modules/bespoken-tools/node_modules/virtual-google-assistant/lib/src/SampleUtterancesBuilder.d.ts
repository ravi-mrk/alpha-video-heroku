import { SampleUtterances } from "virtual-core";
export declare class SampleUtterancesBuilder {
    static fromDirectory(directory: string): SampleUtterances;
    private static extractUtterancesFromFile;
}
