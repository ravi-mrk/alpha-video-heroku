export declare class BespokeServer {
    private webhookPort;
    private nodePorts;
    private nodeManager;
    private webhookManager;
    private uncaughtExceptionHandler;
    constructor(webhookPort: number, nodePorts: number[]);
    start(): Promise<void>;
    stop(): Promise<void>;
}
